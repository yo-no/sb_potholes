require "spec_helper"

describe SbPotholes do
  it "has a version number" do
    expect(SbPotholes::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
